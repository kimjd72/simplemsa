# bootstrap-alpha.yml
spring:
  profile: alpha
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kimjd72/simple-msa
          search-paths: config-server/src/main/resources/server-configs