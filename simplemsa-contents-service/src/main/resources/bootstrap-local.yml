# contents-service bootstrap-local.yml 
spring:
  profiles: local
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: configuration-server
        enabled: true
#      uri: http://localhost:9000